language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

before_install:
    - sudo apt-get install -y subversion # For Massbank.

branches:
  only:
    - master
    - develop
    - /^release[/].*$/

script:
    - DATABASES=mass.csv.file MODES=online make test
    - make check
    - make test && MODES=online make test && sudo make install && make ex
