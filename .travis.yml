language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

env:
    - CHECK=yes
    - MODES=offline
    - MODES=online DATABASES=hmdb.metabolites
    - MODES=online DATABASES=massbank.jp
    - MODES=online DATABASES=peakforest.mass
    - MODES=online DONT_TEST_DBS=hmdb.metabolites,massbank.jp,peakforest.mass
    - MODES=quick.online

branches:
  only:
    - master
    - develop
    - /^release[/].*$/
    - /^testing[/]travis-.*$/

script:
    - ./travis-tests
