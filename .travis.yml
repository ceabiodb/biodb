language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

branches:
  only:
    - master
    - develop
    - /^release[/].*$/

script:
    - make check
    - make test && MODES=online make test && sudo make install && make ex
