language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

env:
    - CHECK=yes
    - DATABASES=hmdb.metabolites MODES=offline,online
    - DATABASES=massbank MODES=offline,online
    - DATABASES=peakforest.mass MODES=offline,online
    - MODES=offline DONT_TEST_DBS=hmdb.metabolites,massbank,peakforest.mass
    - MODES=online DONT_TEST_DBS=hmdb.metabolites,massbank,peakforest.mass

branches:
  only:
    - master
    - develop
    - /^release[/].*$/
    - /^testing[/]travis-.*$/

script:
    - ./travis-tests
