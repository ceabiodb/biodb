language: r
cache: packages
r:
  - bioc-devel

env:
    - BIODB_USERAGENT="biodb.test ; pierrick.roger@cea.fr"

notifications:
    email:
        recipients:
            - pierrick.roger@cea.fr

branches:
  only:
    - master
    
before_install:
    - R -e 'install.packages("BiocManager");BiocManager::install("BiocCheck")'

script:
    - make bioc.check.clone
    - make bioc.check
