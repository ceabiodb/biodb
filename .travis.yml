language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

env:
#    - CHECK=yes EXAMPLES=yes MODES=offline
    - CHECK=no  EXAMPLES=no  DATABASES=hmdb.metabolites  FUNCTIONS=test.wrong.entry MODES=online
#    - CHECK=no  EXAMPLES=no                 DONT_TEST_DBS=hmdb.metabolites  MODES=online
#    - CHECK=no  EXAMPLES=no  MODES=quick.online

branches:
  only:
    - master
    - develop
    - /^release[/].*$/
    - /^testing[/]travis-.*$/

script:
    - ./travis-tests
