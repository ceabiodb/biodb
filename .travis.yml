language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

env:
    - MODES=offline
    - MODES=online
    - MODES=quick.online

branches:
  only:
    - master
    - develop
    - /^release[/].*$/

script:
    - make check
    - make test && make install && make ex
