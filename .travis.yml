language: r
cache: packages

notifications:
 email:
  recipients:
   - pk.roger@icloud.com

env:
    - CHECK=yes EXAMPLES=yes MODES=offline
    - CHECK=no  EXAMPLES=no  MODES=online
    - CHECK=no  EXAMPLES=no  MODES=quick.online

branches:
  only:
    - master
    - develop
    - /^release[/].*$/

script:
    - ./travis-tests
