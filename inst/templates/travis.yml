# Travis-CI configuration file for biodb extensions packages, version 1.0.0

language: r
cache: packages
r: release

env:
    - BIODB_USERAGENT="{{pkgName}}.test ; {{email}}"

notifications:
    email:
        recipients:
            - {{email}}

branches:
  only:
    - master
    
before_install:
    - R -e 'install.packages("BiocManager");BiocManager::install("BiocCheck")'

script:
    - make check

#after_success:
#    - Rscript -e 'covr::codecov()'
