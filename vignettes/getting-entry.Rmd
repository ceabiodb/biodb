<!--
%\VignetteEngine{knitr::rmarkdown}
%\VignetteIndexEntry{Obtaining an entry from a database}
-->

# Obtaining an entry from a database

Here's how to obtain an entry, knowing its accession number:
```{r}
# Create a Biodb instance
mybiodb <- biodb::Biodb$new()

# Request the entry 2528 in ChEBI
entry <- mybiodb$getFactory()$getEntry('chebi', id = '2528')
```

```{r}
print(entry$getFieldValue('accession'))
```
